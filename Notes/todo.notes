-- NEED to edit death rate to be per captia

-- nursing homes per capita

-- take max rates of dist/undist and multiply by population, add to output spreadsheet

-- add a measure of centrality to the peaks vs. cases per capita graph (quartiles stuff)

-- plot peaks

-- start thinking about other dates

-- send raw data (with start/end dates back in)